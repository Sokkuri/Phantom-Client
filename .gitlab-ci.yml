image: "wgamedia/phantom-client-continuous:latest"

before_script:
    - npm install

stages:
    - build

build:
    stage: build
    script:
        - npm run lint
        - npm run build

cache:
    paths:
        - Phantom.Client/node_modules/
